# Exporting and Versioning

Every project starts at mod version `0.0.0`.

Before each export:

1. Pick a bump type (patch, minor, major).
2. Confirm mod metadata.
3. Export ZIP.

The exported package includes a mod descriptor and generated defines file.
